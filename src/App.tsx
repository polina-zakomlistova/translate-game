import Routes from './Routes';

import './App.scss';
import { FC } from 'react';

const App: FC = () => {
    return (
        <div className="app">
            <Routes />
        </div>
    );
};

export default App;
